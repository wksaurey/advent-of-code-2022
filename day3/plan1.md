```
for every line in file
    string one = first half
    string two = second half
    for letter in string one
        if in string one
            get result with ascii modification
            add result to total
            continue (break this loop and continue with the next iteration)
```
